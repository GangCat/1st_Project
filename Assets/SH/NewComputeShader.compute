#pragma kernel CopyTexture

Texture2D<float4> _InputTex;
RWTexture2D<float4> _BackBuffer;

[numthreads(8, 8, 1)]
void CopyTexture(uint3 id : SV_DispatchThreadID)
{
    float4 color = _InputTex.Load(int3(id.xy, 0)); 
    if (color.a > 0)
    {
        _BackBuffer[id.xy] = color; 
    }
}
